<% if user_signed_in? %>
<p>Welcome, <%= current_user.first_name %> <%= current_user.last_name %></p>
  <li>
  <%= link_to('Home', '/') %>
  </li>
  <li>
  <%= link_to('Edit profile', edit_user_registration_path) %>
  </li>
  <li>
  <%= link_to('Compose', '/compose') %>
  </li>

<h1>Listing Users</h1>

<table>
  <thead>Active Users
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <% @okays.each do |user| %>
      <tr>
        <td><%= user.last_name %>, <%= user.first_name %></td>
        <td><%= link_to 'Show', user %></td>
        <% if user.status == "OK" %>
        <td><%= link_to 'Disable', disable_user_path(user) %></td>
        <% end %>
        <% if user.status == "DISABLED" %>
        <td><%= link_to 'Restore', restore_user_path(user) %></td>
        <% end %>
        <td><%= link_to 'Terminate', terminate_user_path(user) %></td>
        <td><%= link_to 'Delete', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <% end %>
  </tbody>
</table>
<br />
<table>
  <thead>Disabled Users
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <% @disableds.each do |user| %>
      <tr>
        <td><%= user.last_name %>, <%= user.first_name %></td>
        <td><%= link_to 'Show', user %></td>
        <% if user.status == "OK" %>
        <td><%= link_to 'Disable', disable_user_path(user) %></td>
        <% end %>
        <% if user.status == "DISABLED" %>
        <td><%= link_to 'Restore', restore_user_path(user) %></td>
        <% end %>
        <td><%= link_to 'Terminate', terminate_user_path(user) %></td>
        <td><%= link_to 'Delete', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <% end %>
  </tbody>
</table>
<br />
<table>
  <thead>Terminated Users
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <% @terminateds.each do |user| %>
      <tr>
        <td><%= user.last_name %>, <%= user.first_name %></td>
        <td><%= link_to 'Show', user %></td>
        <% if user.status == "OK" %>
        <td><%= link_to 'Disable', disable_user_path(user) %></td>
        <% end %>
        <% if user.status == "DISABLED" %>
        <td><%= link_to 'Restore', restore_user_path(user) %></td>
        <% end %>
        <td><%= link_to 'Terminate', terminate_user_path(user) %></td>
        <td><%= link_to 'Delete', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <% end %>
  </tbody>
</table>

<br>

<% if current_user.admin_check == true %>
<%= link_to 'New User', new_user_path %>
<% end %>
<%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
<% end %>
